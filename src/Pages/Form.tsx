import React from 'react';
import {FormModule} from '../components/Modules/FormPageModules/FormModule';

export const Form = () => {
    return (
        <div>
            <FormModule/>
        </div>
    );
};

export default Form;
